meta {
  name: Reject Access Request
  type: http
  seq: 6
}

patch {
  url: {{base_url}}/access-requests/{{access_request_id}}/reject
  body: none
  auth: bearer
}
auth:bearer {
  token: {{token}}
}
