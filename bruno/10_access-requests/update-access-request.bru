meta {
  name: Update Access Request
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/access-requests/{{access_request_id}}
  body: json
  auth: bearer
}
body:json {
  {
    "description": "Updated description"
  }
}

auth:bearer {
  token: {{token}}
}
