meta {
  name: Login Audit
  type: http
  seq: 16
}

get {
  url: {{base_url}}/dashboard/login-audit
  body: none
  auth: bearer
}
params:query {
  ~success: 
  ~email: 
  offset: 0
  limit: 50
}

auth:bearer {
  token: {{token}}
}
