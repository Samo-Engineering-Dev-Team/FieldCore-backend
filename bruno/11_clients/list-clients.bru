meta {
  name: List Clients
  type: http
  seq: 2
}

get {
  url: {{base_url}}/clients/
  body: none
  auth: bearer
}
params:query {
  active_only: true
  offset: 0
  limit: 50
}

auth:bearer {
  token: {{token}}
}
