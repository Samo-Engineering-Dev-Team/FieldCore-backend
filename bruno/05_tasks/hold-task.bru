meta {
  name: Hold Task
  type: http
  seq: 8
}

patch {
  url: {{base_url}}/tasks/{{task_id}}/hold
  body: json
  auth: bearer
}
body:json {
  {
    "reason": "Waiting for parts"
  }
}

auth:bearer {
  token: {{token}}
}
