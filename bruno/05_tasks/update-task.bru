meta {
  name: Update Task
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/tasks/{{task_id}}
  body: json
  auth: bearer
}
body:json {
  {
    "description": "Updated description"
  }
}

auth:bearer {
  token: {{token}}
}
