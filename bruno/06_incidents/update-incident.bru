meta {
  name: Update Incident
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/incidents/{{incident_id}}
  body: json
  auth: bearer
}
body:json {
  {
    "description": "Updated description",
    "severity": "critical"
  }
}

auth:bearer {
  token: {{token}}
}
