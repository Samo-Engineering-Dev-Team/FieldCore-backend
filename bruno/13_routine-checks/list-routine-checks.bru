meta {
  name: List Routine Checks
  type: http
  seq: 2
}

get {
  url: {{base_url}}/routine-checks/
  body: none
  auth: bearer
}
params:query {
  ~status: 
  offset: 0
  limit: 50
}

auth:bearer {
  token: {{token}}
}
